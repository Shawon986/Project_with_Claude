# Runtime dependencies for the Vercel API (api/index.py).
# Kept intentionally minimal so the serverless function bundle stays small.
# The full local analysis pipeline dependencies live in requirements-full.txt.
fastapi>=0.109.0
mangum>=0.17.0
